# PractiScoreResultsUploader

A test app for eventually developing an Android app for uploading shooting competition score data from PractiScore app to a server. The app is intended for testing the reading of score data from PractiScore export files and sending the data via HTTP request, before moving the code to Android and adding a UI.
